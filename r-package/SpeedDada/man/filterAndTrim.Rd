% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SpeedDada.R
\name{filterAndTrim}
\alias{filterAndTrim}
\title{Filter and Trim FASTQ Reads}
\usage{
filterAndTrim(
  fwd,
  filt,
  rev = NULL,
  filt.rev = NULL,
  truncLen = 0L,
  trimLeft = 0L,
  maxEE = Inf,
  truncQ = 2L,
  minLen = 20L,
  compress = TRUE,
  multithread = FALSE,
  ...
)
}
\arguments{
\item{fwd}{Character vector of forward-read input paths.}

\item{filt}{Character vector of forward-read output paths.}

\item{rev}{Character vector of reverse-read input paths, or \code{NULL}
for single-end.}

\item{filt.rev}{Character vector of reverse-read output paths, or \code{NULL}.}

\item{truncLen}{Integer of length 1 or 2: truncation length per direction.}

\item{trimLeft}{Integer of length 1 or 2: bases to trim from the 5' end.}

\item{maxEE}{Numeric of length 1 or 2: maximum expected errors.}

\item{truncQ}{Integer: truncate at first base below this Phred score.}

\item{minLen}{Integer: discard reads shorter than this after truncation.}

\item{compress}{Accepted for compatibility; output is plain FASTQ.}

\item{multithread}{Accepted for compatibility; Rust uses Rayon automatically.}

\item{...}{Extra arguments ignored for compatibility.}
}
\value{
Integer matrix of dimensions [files x 2] with column names
  \code{c("reads.in", "reads.out")}.
}
\description{
Drop-in replacement for \code{dada2::filterAndTrim}. Filters and trims
single- or paired-end FASTQ files using the high-performance Rust core.
}
\examples{
fwd_in  <- system.file("extdata", "sample_R1.fastq", package = "SpeedDada")
fwd_out <- tempfile(fileext = ".fastq")
filterAndTrim(fwd_in, fwd_out, truncLen = 30, maxEE = 5, minLen = 10)

}
