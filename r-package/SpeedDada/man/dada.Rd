% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SpeedDada.R
\name{dada}
\alias{dada}
\title{Denoise Amplicon Reads}
\usage{
dada(
  derep,
  err,
  selfConsist = FALSE,
  pool = FALSE,
  omega_a = 1e-40,
  multithread = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{derep}{A \code{"derep"} object from \code{\link{derepFastq}}, or a
list of them for multi-sample denoising.}

\item{err}{Error model from \code{\link{learnErrors}}.}

\item{selfConsist}{Logical: not implemented; emits a warning if \code{TRUE}.}

\item{pool}{Logical or \code{"pseudo"}: \code{TRUE} enables cross-sample
pooling; \code{"pseudo"} uses Callahan's two-pass pseudo-pooling.}

\item{omega_a}{Numeric: DADA significance threshold (default \code{1e-40}).}

\item{multithread}{Accepted for compatibility; Rust uses Rayon automatically.}

\item{verbose}{Logical: ignored.}

\item{...}{Extra arguments ignored.}
}
\value{
A \code{"dada"} object with \code{$denoised} (named integer vector
  of sequence → count), or a list of such objects if \code{derep} is a list.
}
\description{
Drop-in replacement for \code{dada2::dada}. Runs the DADA partition + EM
refinement algorithm on dereplicated samples.
}
\examples{
fastq <- system.file("extdata", "sample_R1.fastq", package = "SpeedDada")
d <- derepFastq(fastq)
err <- learnErrors(fastq, nbases = 1e4)
res <- dada(d, err, omega_a = 1e-5)
head(res$denoised)

}
