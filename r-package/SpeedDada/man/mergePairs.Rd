% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SpeedDada.R
\name{mergePairs}
\alias{mergePairs}
\title{Merge Paired-End Reads}
\usage{
mergePairs(
  dadaF,
  derepF,
  dadaR,
  derepR,
  minOverlap = 12L,
  maxMismatch = 0L,
  justConcatenate = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{dadaF}{\code{"dada"} object for forward reads.}

\item{derepF}{\code{"derep"} object for forward reads (accepted for compatibility).}

\item{dadaR}{\code{"dada"} object for reverse reads.}

\item{derepR}{\code{"derep"} object for reverse reads (accepted for compatibility).}

\item{minOverlap}{Integer: minimum overlap length (default 12).}

\item{maxMismatch}{Integer: maximum mismatches in overlap (default 0).}

\item{justConcatenate}{Logical: concatenate instead of merging.}

\item{verbose}{Logical: ignored.}

\item{...}{Extra arguments ignored.}
}
\value{
Data frame with columns \code{sequence}, \code{abundance},
  \code{accept}, \code{nmatch}, \code{nmismatch}, \code{nindel}.
}
\description{
Drop-in replacement for \code{dada2::mergePairs}.
}
\examples{
fwd <- system.file("extdata", "sample_R1.fastq", package = "SpeedDada")
rev <- system.file("extdata", "sample_R2.fastq", package = "SpeedDada")
dF <- derepFastq(fwd); dR <- derepFastq(rev)
err <- learnErrors(c(fwd, rev), nbases = 1e4)
aF <- dada(dF, err, omega_a = 1e-5); aR <- dada(dR, err, omega_a = 1e-5)
mergePairs(aF, dF, aR, dR, minOverlap = 5)

}
